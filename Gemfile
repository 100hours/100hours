source 'https://rubygems.org'
gem 'rails', '~> 4.0.10' # This change was made via Snyk to fix a vulnerability

gem 'jquery-rails'
gem 'thin', '~> 1.5.0' # This change was made via Snyk to fix a vulnerability
gem 'pg'
gem 'haml'
gem 'bootstrap-sass'
gem 'bourbon'

gem 'pusher'
gem 'ember-rails'

gem 'newrelic_rpm'

group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
  gem 'uglifier', '~> 2.7.2' # This change was made via Snyk to fix a vulnerability
end

group :development, :test do
  gem 'quiet_assets', '~> 1.0.1' # This change was made via Snyk to fix a vulnerability
  gem 'haml-rails'
  gem 'hpricot'
  gem 'ruby_parser', '~> 3.1.2' # This change was made via Snyk to fix a vulnerability
  gem 'rspec-rails'
  gem 'capybara', '~> 2.0.1' # This change was made via Snyk to fix a vulnerability
  gem 'database_cleaner'
  gem 'email_spec', '~> 1.4.0' # This change was made via Snyk to fix a vulnerability
  gem 'factory_girl_rails', '~> 4.1.0' # This change was made via Snyk to fix a vulnerability
  gem 'foreman'
end
